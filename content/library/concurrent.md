# The `concurrent` package

This package contains the following modules:

* [`concurrent.futures`](concurrent.futures.md#module-concurrent.futures) – Launching parallel tasks
* [`concurrent.interpreters`](concurrent.interpreters.md#module-concurrent.interpreters) – Multiple interpreters in the same process

<!-- Apparently this how you hack together a formatted link:
(https://www.docutils.org/docs/ref/rst/directives.html#replacement-text) -->
